# Text mining — viste aggregate e analisi. Python >= 3.9 (consigliato 3.12).
# Baseline per aggregati / notebook / data viz; le librerie specifiche delle
# analisi (topic modeling, NLP, dashboard) si aggiungono man mano.
pandas>=2.0
numpy>=1.24
pyarrow>=14
matplotlib>=3.7
seaborn>=0.13               # heatmap delle analisi
jupyter

# I notebook in analisi/ si appoggiano sull'indice del vector database (repo
# gemello) importando vdb: servono le sue dipendenze.
sentence-transformers>=2.2  # embedding e5 (porta torch)
lancedb>=0.24               # store + ricerca ibrida dell'indice
py3langid>=0.3              # importato da vdb
